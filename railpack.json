{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "22"
  },
  "steps": {
    "install": {
      "commands": [
        "if [ -d backend ]; then cd backend; fi && npm install --omit=dev"
      ]
    },
    "build": {
      "inputs": [
        { "step": "install" }
      ],
      "commands": ["true"]
    }
  },
  "deploy": {
    "startCommand": "if [ -d backend ]; then node backend/server.js; else node server.js; fi",
    "variables": {
      "TZ": "Asia/Karachi"
    }
  }
}
